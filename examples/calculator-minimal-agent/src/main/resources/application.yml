spring:
  application:
    name: calculator-minimal-agent-example

ai:
  agent:
    model-provider: demo
    model-id: calculator-minimal-scripted
    system-prompt: "You are a calculator assistant. Use the calculator tool for roots."
    max-iterations: 8

server:
  port: 8089
